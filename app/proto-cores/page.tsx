// PROTOTYPE ROUTE — throwaway. Compare every core graphic in one place to
// settle the #4 centerpiece decision. Delete this folder + the component when
// done — see ./NOTES.md.
import CoreGalleryPrototype from "@/components/CoreGalleryPrototype";

export const metadata = {
  title: "CORE GALLERY (prototype) — compare every graphic",
};

export default function ProtoCoresPage() {
  return <CoreGalleryPrototype />;
}
